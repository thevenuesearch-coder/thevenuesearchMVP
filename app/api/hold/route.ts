import {NextResponse} from 'next/server';import {supabase} from '../../../lib/supabase';export async function POST(req:Request){const body=await req.json();const {data,error}=await supabase.rpc('create_venue_hold',{p_venue_id:body.venue_id,p_event_date:body.event_date,p_user_id:body.user_id,p_guest_count:body.guest_count,p_event_type:body.event_type});if(error)return NextResponse.json({error:error.message},{status:409});return NextResponse.json({data})}
