import {NextResponse} from 'next/server';import {supabase} from '../../../lib/supabase';export async function POST(req:Request){const body=await req.json();const {data,error}=await supabase.from('booking_requests').insert({venue_id:body.venue_id,user_id:body.user_id,event_date:body.event_date,guest_count:body.guest_count,event_type:body.event_type,mode:body.mode,notes:body.notes||null}).select().single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data})}
